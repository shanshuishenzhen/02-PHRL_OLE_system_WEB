from django.http import JsonResponse
from rest_framework.decorators import api_view

# 这里将实现阅卷中心的视图函数
# @api_view(['GET'])
# def marking_papers_list(request):
#     return JsonResponse({'message': '获取待阅试卷列表成功'})